import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Elite_Schedule_App',
  appName: 'Elite_Schedule_App',
  webDir: 'www'
};

export default config;
